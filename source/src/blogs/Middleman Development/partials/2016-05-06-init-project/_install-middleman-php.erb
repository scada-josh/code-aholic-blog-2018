

### 2. ติดตั้ง `middleman-php`  ![separate-line-sub-topic][common-07] 

#### 2.1) ไฟล์ Gemfile
---
<pre class="prettyprint linenums">
# If you do not have OpenSSL installed, update
# the following line to use "http://" instead
source 'https://rubygems.org'

gem "middleman", "~>3.3.12"

# Live-reloading plugin
gem "middleman-livereload", "~> 3.1.0"

# For faster file watcher updates on Windows:
gem "wdm", "~> 0.1.0", :platforms => [:mswin, :mingw]

# Windows does not come with time zone data
gem "tzinfo-data", platforms: [:mswin, :mingw, :jruby]

<mark>gem "middleman-php"</mark>
</pre>

<br/>

<pre class="prettyprint linenums">
$ bundle install
</pre>

หรือ หากต้องการ `Update Gemfile` ให้ใช้คำสั่งต่อไปนี้

<pre class="prettyprint linenums">
$ bundle update
</pre>

<br/>


#### 2.2) ไฟล์ config.rb
---
<pre class="prettyprint linenums">
# Reload the browser automatically whenever files change
 configure :development do
#   activate :livereload
    <mark>activate :php</mark>
 end


# Build-specific configuration
configure :build do
  <mark>activate :php</mark>
end
</pre>

<br/>

#### 2.3) ทดลองสร้างไฟล์ about.php.erb
---

<br/>

#### 2.4) รันคำสั่ง Build and Deploy
---

<pre class="prettyprint linenums">
$ bundle exec middleman build --verbose
</pre>

สังเกตผลลัพธ์ จะได้ไฟ์ about.php เรียบร้อยแล้ว

<br/>