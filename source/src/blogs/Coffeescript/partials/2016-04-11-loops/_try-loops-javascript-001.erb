<pre class="prettyprint linenums">
var food, foods, i, j, k, l, len, len1, len2, len3, m;

for (j = 0, len = arr.length; j < len; j++) {
  i = arr[j];
  console.log(i);
}

foods = ['cake', 'ice cream'];

for (k = 0, len1 = foods.length; k < len1; k++) {
  food = foods[k];
  console.log(food);
}

for (l = 0, len2 = foods.length; l < len2; l++) {
  food = foods[l];
  console.log(food);
}

for (i = m = 0, len3 = foods.length; m < len3; i = ++m) {
  food = foods[i];
  console.log(i + food);
}
</pre>