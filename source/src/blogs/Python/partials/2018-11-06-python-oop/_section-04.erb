
`การสืบทอดคลาส (Inheritance)` เป็นการสร้างคลาสใหม่่จากคลาสที่มีอยู่ ซึ่งคลาสใหม่จะมีคุณสมบัติและเมธอดของคลาสเดิมทั้งหมด รวมทั้งสามารถสร้างคุณสมบัติและเมธอดเพิ่มเติมได้เอง

<pre class="prettyprint linenums">
# -*- coding: utf-8 -*- 

class Shape:
	"""Parent Class"""		# สร้างคลาสแม่ชื่อ Shape
	def __init__(self, width, height):
		self.width = width
		self.height = height

class Rectangle(Shape):
	def area(self):
		return self.width * self.height


print 'Area of Reactangle Calculation \n Please enter values for calculate.'
w = int(input('Enter width : '))
h = int(input('Enter height : '))

rect = Rectangle(w, h)
print 'Rectangle area : ', rect.area()	

</pre>

